<!DOCTYPE html>
<html>
  <head>
    <title>FindReplace!</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    <link rel="stylesheet" href="/Content/css/styles.css" media="screen" title="no title" charset="utf-8">
  </head>
  <body>
    <h1>Here are your results:</h1>

    <p> Your initial phrase is: <span class="output">@Model.GetInput1() </span></p>
    <p>The word to be replaced is: <span class="output">@Model.GetInput2()</span></p>
    <p>You replaced that word with: <span class="output">@Model.GetInput3()</span></p>
    <p>Your final phrase is:<span class="output">@Model.Result() </span></p>
    <form method="get" action="/">
      <button type="submit" name="button" class="btn btn-primary">Start over!</button>
    </form>
  </body>
</html>
